# Resolved indirect jump targets in X.EXE.
#
# Format: SEG:OFF  # justification
# Read automatically by tools/re/disasm.py via --seeds.

# ---------------------------------------------------------------------------
# call word ptr cs:[bx-0x2BC6] at 0000:D2DA
# Table base (0 - 0x2BC6) & 0xFFFF = 0xD43A, 7 entries.
# Index comes from `and bl,0x0F / shl bl,1`.
# Every handler tests cs:[0xD5C8] against 4 -> graphics adapter class.
# This is the pixel format dispatcher (CGA/Tandy/EGA/VGA), not a decompressor.
# ---------------------------------------------------------------------------
0000:D524   # entry 0 - ret, empty slot
0000:D448   # entry 1 - cx=0x2000 (8 KB plane)
0000:D456   # entry 2 - cx=0x2880
0000:D496   # entry 3 - cx=0x3C00, bitplane expansion via rcr
0000:D4C7   # entry 4
0000:D4F2   # entry 5
0000:D525   # entry 6 - chain: D448, D456, D448, D496

# ---------------------------------------------------------------------------
# jmp ax at 0000:D2EE - `mov ax,0xD329` sits immediately before it
# ---------------------------------------------------------------------------
0000:D329

# ---------------------------------------------------------------------------
# call word ptr [si] at 0000:29D5
# `mov si,0x29EF` at 0x29CC. The table is walked bit by bit through AL
# (shl al,1 / jae skip), so at most 8 entries - a subsystem init mask.
# Called with al=0xFF at 0x004F, so all subsystems.
# ---------------------------------------------------------------------------
0000:26F0   # bit 3
0000:26F5   # bit 4
0000:2836   # bit 5
0000:2916   # bit 6
0000:29B8   # bit 7 - int 15h AX=0xC200, PS/2 mouse
# Bits 0-2 point at 0x29DF (ret), empty slots

# ---------------------------------------------------------------------------
# jmp ax at 0000:5B8C - text interpreter, control codes 0..9
# `shl ax,1 / add ax,0x5BE6` with ax = sign_extend(al-9), so the table runs
# backwards from 0x5BE6: code 9 -> 0x5BE6, code 0 -> 0x5BD4.
# Slots 2..8 are short jmp trampolines, slot 9 is code.
# ---------------------------------------------------------------------------
0000:5BD6   # code 1 - pop bx / ret
0000:5BA4   # code 2
0000:5BAC   # code 3
0000:5BB1   # code 4
0000:5BB8   # code 5
0000:5BBC   # code 6
0000:5BC1   # code 7
0000:5BCA   # code 8
0000:5BE6   # code 9 - lodsb / xor ah,ah / shl ax,1 / mov dx,ax

# ---------------------------------------------------------------------------
# jmp bp at 0000:5B92 - glyph renderer, preselected by `mov bp,imm`
# Characters 0x20..0x67 end up here.
# ---------------------------------------------------------------------------
0000:58FB   # via 0x5B4C
0000:5852   # via 0x5B51
0000:599A   # via 0x5B56 and 0x5B6B

# ---------------------------------------------------------------------------
# jmp word ptr [bx] at 0000:E4CC - menu dispatcher
#
# Sequence: `mov cl,N` sets the entry count, `call 0xE49F` reads a key
# ('1'..'9','0' -> index 0..9) and checks it with `cmp al,cl`, then
# `call 0xE4C5` dispatches through a table that sits INLINE after the call
# site (`pop bx / add bx,ax / jmp [bx]`).
#
# Three call sites, found by byte scan (tools/re/xref.py E4B9-E4CC).
# ---------------------------------------------------------------------------

# Call site 0000:C561, table 0xC564, 7 entries.
# Bounded by `jb 0xC572` at 0xC55F, which makes 0xC572 code.
0000:C6C5
0000:C58E
0000:C5B4

# Call site 0000:D751, table 0xD754, 8 entries (`mov cl,8` at 0xD74A).
0000:D872
0000:E199
0000:DC02
0000:E106
0000:DCB5
0000:D766
0000:E6F3
0000:74CA

# Call site 0000:E67D, table 0xE680, 7 entries.
# Bounded by the inline-string routine starting at 0xE68E (`mov al,8`).
0000:D676
0000:874E
0000:74E0
0000:7051
0000:6F0B
0000:E6AB

# ---------------------------------------------------------------------------
# call word ptr [bx-0x70C4] at 0000:8F26 -> base (0-0x70C4)&0xFFFF = 0x8F3C
# 4 entries: the first handler begins at 0x8F44, which bounds the table.
# ---------------------------------------------------------------------------
0000:8F44
0000:8F94
0000:8F99
0000:9312

# ---------------------------------------------------------------------------
# call word ptr [bx-0x5929] at 0000:F263 -> base (0-0x5929)&0xFFFF = 0xA6D7
# Index from `mov bx,[0xA720] / shl bl,1`.
# 13 entries: from 0xA6F1 on there is 0x5C58, ASCII "00" and zeros, no pointers.
# All 13 targets lie in 0xA8F9..0xAE09, so they are plausibly contiguous.
# ---------------------------------------------------------------------------
0000:A9BC
0000:A93B
0000:AE09
0000:ADD3
0000:ADE7
0000:ADC2
0000:ADF7
0000:AD98
0000:ADB0
0000:A979
0000:A95A
0000:A8F9

# ---------------------------------------------------------------------------
# Unresolved - no fixed target:
#
# jmp si at 0000:5B60 / 0000:5B66
#   Inline-string idiom: `pop si` takes the return address as a pointer to a
#   string sitting right after the call, and `jmp si` resumes past the string
#   terminator. The target differs per call site.
#   String end = byte with bit 7 set.
#
# jmp word ptr [bx] at 0000:E4CC
#   `pop bx / add bx,ax / jmp [bx]` - the jump table sits inline after each
#   call site. The target differs per call site.
#
# lcall [0xFC14], 35 sites
#   Points out of X.EXE into a loaded overlay, see docs/ARCHIVE-FORMAT.md.
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# call bx at 0000:9849, inside the 0x9811 scan loop called from 0xCC8C.
# bx is loaded from a single, unconditional `mov bx,0x9930` at 0000:9811 -
# not a table, a plain function pointer. Confirmed by decoding the raw bytes
# at 0x9930 directly: 14 sensible instructions ending in `ret`, immediately
# followed by more clean code (a per-theatre table lookup at 0x9C84, mirroring
# the one at 0x75FC documented in RE-NOTES.md). See docs/GAME-LOOP.md.
# ---------------------------------------------------------------------------
0000:9930
